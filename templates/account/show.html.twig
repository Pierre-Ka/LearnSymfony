{% extends 'layouts/base.html.twig' %}

{% block title 'Profile' %}

{% block body %}


<div class="row">
    <div class="col-md-6 mx-auto text-center">
        <h1 class="my-5"> Profile </h1>

        {#
            On utilise gravatar pour l'image , Gravatar est un service de centralisation d'avatar.
            https://en.gravatar.com/site/implement/images/php/
            on doit entrer notre email en md5()
            <img src="https://www.gravatar.com/avatar/md5(email)/?s=taille" alt="">
            <img src="https://www.gravatar.com/avatar/2715122e946d15db11ea3a339533f8ff/?s=100" alt="">
            On crée une propriété getGravatar dans la table User.
        #}

        <img src="{{ app.user.gravatarUrl }}" alt="{{ app.user.fullName }}'s Avatar" class="rounded-circle shadow">

        <p class="mt-5 lead">{{ app.user.email }}</p>
        <hr>

        <h2>{{ app.user.fullName }}</h2>
        <p><small> Account created At {{ app.user.createdAt|date('d/m/Y') }}</small></p>
        <p>
            <a href=" {{ path('app_account_edit') }}">Edit Account </a> | <a href=" {{ path('app_account_change_password') }} ">Change password </a>
        </p>

    </div>

</div>
{% endblock %}
